import { Routes } from '@angular/router';
import { AddressFormComponent } from './address-form/address-form.component';

export const routes: Routes = [{ path: 'address-form', component: AddressFormComponent }];
